import ServicesAll from "@/components/ServicesAll";

export const metadata = {
  title: "Services | Bright Path",
  description: "All our services on one page: placement, visa, accommodation, airport pickup.",
};

export default function Page() {
  return <ServicesAll lang="en" />;
}
